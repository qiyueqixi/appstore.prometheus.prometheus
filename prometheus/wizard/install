[
    {
        "stepTitle": "${install.basic_auth_users_title}",
        "items": [
            {
                "type": "tips",
                "helpText": "${install.basic_auth_users_help}"
            },
            {
                "type": "switch",
                "field": "wizard_enable_basic_auth_users",
                "label": "${install.enable_basic_auth_users}"
            },
            {
                "type": "text",
                "field": "wizard_username",
                "label": "${install.username_label}",
                "initValue": "username",
                "rules": [
                    {
                        "required": true,
                        "message": "${install.username_required}"
                    },
                    {
                        "pattern": "^[a-zA-Z0-9]+$",
                        "message": "${install.username_pattern}"
                    }
                ]
            },
            {
                "type": "text",
                "field": "wizard_password",
                "label": "${install.password_label}",
                "initValue": "password",
                "rules": [
                    {
                        "required": true,
                        "message": "${install.password_required}"
                    },
                    {
                        "pattern": "^[a-zA-Z0-9]+$",
                        "message": "${install.password_pattern}"
                    }
                ]
            }
        ]
    }
]
